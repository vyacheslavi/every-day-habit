events {
	use epoll;
	worker_connections 1024;
}

error_log /var/log/nginx/error.log;


http {

  include /etc/nginx/mime.types;

  server {
    listen 80;
    server_name ${APP_DOMAINNAME};

    location / {
        resolver 8.8.8.8 ipv6=off;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        set $app_container app_container:8000;

        proxy_pass http://$app_container;
      }
  }
}
# http {
#   server {
#     listen 80 default_server;
#     listen [::]:80 default_server;
#     server_name _;

#     location /.well-known/acme-challenge {
#       resolver 127.0.0.11 valid=30s;
#       set $upstream letsencrypt;
#       proxy_pass http://$upstream:80;
#       proxy_set_header Host            $host;
#       proxy_set_header X-Forwarded-For $remote_addr;
#       proxy_set_header X-Forwarded-Proto https;
#     }

# 	# server_name edh.vyacheslavi.ru;
# 	# return 301 https://${DOMAINNAME}$request_uri;

#     location / {
#       return 301 https://$host$request_uri;
#     }
#   }

#   server {
#     listen 443 ssl;
#     server_name ${APP_DOMAINNAME};

#     ssl_certificate /etc/letsencrypt/live/${APP_DOMAINNAME}/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/${APP_DOMAINNAME}/privkey.pem;
#     ssl_session_timeout 5m;
#     ssl_protocols TLSv1.2;
#     ssl_ciphers 'EECDH+AESGCM: EDH+AESGCM:AES256+EECDH:AES256+EDH';
#     ssl_prefer_server_ciphers on;

#     ssl_session_cache shared:SSL:10m;
#     ssl_dhparam dhparam.pem;

#     location / {
#         proxy_set_header        Host $host;
#         proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header        X-Forwarded-Proto $scheme;

#         # use the mapped hostname and port for a proxy target
#         proxy_pass              "http://$internal_hostname:$internal_port";
#     }
#   }
# }
